services:
  question-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: question-api
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
